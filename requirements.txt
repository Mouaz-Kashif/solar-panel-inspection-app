streamlit==1.36.0
# Streamlit Community Cloud is currently using Python 3.14.
# Avoid pinning to versions that don't provide cp314 wheels.
# Let pip pick compatible wheels for numpy/pillow.

torch==2.12.1
torchvision==0.27.1

numpy
pillow
requests
matplotlib
